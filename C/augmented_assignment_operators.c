
// AUGMENTED ASSIGNMENT OPERATORS:
// help us to reassign values in a short and fast way.

#include <stdio.h>

int main()
{
    int age = 22;

    // age = age + 2;
    // age += 2;

    // age = age - 2;
    // age -= 2;

    // age = age * 2;
    // age *= 2;

    // age = age / 2;
    age /= 2;

    printf("%d \n", age);

    return 0;
}
