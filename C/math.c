

#include <stdio.h>
#include <math.h>

int main()
{
    float a = sqrt(25);
    float b = pow(2, 2);
    int c = round(2.45);
    int d = ceil(2.11);
    int e = floor(2.95);
    float f = log(3);
    float g = tan(45);
    float h = sin(90);

    printf("\n%f\n", h);
    // printf("\n%i\n", f);
}
